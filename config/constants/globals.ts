export const SUM = (a: number, b: number) => {
	return a + b;
};

export type TIconProps = {
	children?: never;
	color?: string;
	height?: string;
	width?: string;
	strokeWidth?: string;
	className?: string;
	stroke?: string;
};
